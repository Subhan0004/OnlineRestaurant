<UserControl x:Class="RestaurantApp.Views.Components.Search"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RestaurantApp.Views.Components"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="800">
    <Grid>
        <DockPanel>
            <Label Content="Search" FontSize="25" FontFamily="Times New Roman" FontStyle="Italic" VerticalAlignment="Center" Margin="30,0,0,0" Width="100" />
            <TextBox Width="600" Height="35" Margin="6 6" VerticalAlignment="Center" HorizontalAlignment="Left">
                <TextBox.Template>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Grid>
                            <Border CornerRadius="5" Background="Crimson">
                                <TextBox Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </TextBox.Template>
            </TextBox>
        </DockPanel>
    </Grid>
</UserControl>
